define("assignsubmission_tipnc/confirmsave",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * Holds back the submit button until the student says the document is saved.
   *
   * The office editor saves in its own time, so submitting right after writing
   * freezes the previous version. This does not detect that —nothing on this side
   * can— but it stops the submission being one click away from a mistake.
   *
   * @module     assignsubmission_tipnc/confirmsave
   * @copyright  2026 3iPunt (contacte@tresipunt.com)
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const SELECTORS_box='[data-region="tipnc-confirmsave"]',SELECTORS_check='[data-action="tipnc-confirmsave"]',SELECTORS_form="#region-main form",SELECTORS_submit='input[type="submit"], button[type="submit"]';_exports.init=()=>{const box=document.querySelector(SELECTORS_box),form=document.querySelector(SELECTORS_form);if(!box||!form)return;const buttons=Array.from(form.querySelectorAll(SELECTORS_submit));if(!buttons.length)return;const primary=buttons[0],row=primary.closest(".d-flex")||primary.closest(".fitem, .form-group, fieldset")||primary.parentNode;row.parentNode.insertBefore(box,row),box.hidden=!1,row.classList.add("tipnc-buttons");const check=box.querySelector(SELECTORS_check);if(!check)return;const toggle=()=>{primary.disabled=!check.checked};check.addEventListener("change",toggle),toggle()}}));

//# sourceMappingURL=confirmsave.min.js.map